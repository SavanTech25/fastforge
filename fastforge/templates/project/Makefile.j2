.PHONY: run test install shell dev

run:
	uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

install:
	uv venv && uv pip install -e .

test:
	uv run pytest

shell:
	uv run ipython

dev:
	uv run fastforge run
