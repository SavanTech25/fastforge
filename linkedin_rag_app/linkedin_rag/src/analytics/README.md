# analytics

Generated by **FastForge ETL ⚡**

This is a dbt project tailored for your data stack.

## Getting Started

1. Set up your environment variables as required by your `profiles.yml` for the `postgres` connector.
2. Run `dbt deps` to install packages (like `dbt_utils`).
3. Run `dbt run` to build your models.
4. Run `dbt test` to execute your tests.