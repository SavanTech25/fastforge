FROM python:{{ python_version }}-slim

WORKDIR /app
COPY pyproject.toml .
COPY src/ src/
RUN pip install --no-cache-dir uv && uv pip install --system .

COPY . .
EXPOSE 8000
CMD ["python", "-m", "{{ package_name }}.api.main"]
