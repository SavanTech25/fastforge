# {{ repo_name }}

[![Python](https://img.shields.io/badge/python-{{ python_version }}-blue.svg)](https://www.python.org/downloads/)
{% if is_open_source %}[![License: {{ open_source_license }}](https://img.shields.io/badge/License-{{ open_source_license }}-yellow.svg)](./LICENSE){% endif %}

> **{{ description }}**

Generated by **FastForge ⚡ ✕ AstroData**.

---

## 📁 Project Structure

```
{{ repo_name }}/
├── app/                    # Streamlit application
│   └── streamlit_app.py
├── data/
│   ├── raw/                # Original, immutable data
│   ├── interim/            # Intermediate transformed data
│   ├── processed/          # Final datasets for modeling
│   └── external/           # Third-party data
├── dockerfiles/            # Multi-stage Dockerfiles
├── docs/                   # Sphinx docs & reports
├── logs/
├── notebooks/              # Jupyter notebooks
├── scripts/                # Utility scripts
├── src/
│   └── {{ package_name }}/
│       ├── api/            # FastAPI implementation
│       │   ├── main.py
│       │   ├── middlewares/
│       │   └── routers/
│       ├── core/           # Shared utilities & config
│       ├── data/           # ETL / data processing
│       ├── features/       # Feature engineering
│       ├── models/         # Training & inference
│       └── visualization/  # Plotting utilities
├── tests/
├── .env.example
├── docker-compose.yml
├── Makefile
└── pyproject.toml
```

## 🚀 Quick Start

```bash
# 1. Install dependencies
make dev-install

# 2. Configure environment
cp .env.example .env

# 3. Run
make run        # Streamlit → http://localhost:8501
make run_api    # FastAPI   → http://localhost:8000/docs
```

## 🐳 Docker

```bash
make up     # Start all services
make down   # Stop all services
```

## 🧪 Quality

```bash
make check    # lint + typecheck + tests
make format   # auto-format
make test     # run pytest
```

## 📄 License

{% if is_open_source %}This project is licensed under the **{{ open_source_license }}** license.{% else %}This project is **proprietary** — all rights reserved.{% endif %}
